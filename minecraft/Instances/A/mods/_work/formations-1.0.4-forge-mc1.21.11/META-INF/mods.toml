# The name of the mod loader type to load - for regular FML @Mod mods it should be javafml
modLoader="javafml" #mandatory
# A version range to match for said mod loader - for regular FML @Mod it will be the forge version
loaderVersion="[61,)" #mandatory
# The license for your mod
license="All rights reserved" #mandatory
# A URL to refer people to when problems occur with this mod
issueTrackerURL="https://github.com/SuperMartijn642/Formations/issues" #optional
# A list of mods - how many allowed here is determined by the individual mod loader
[[mods]] #mandatory
# The modid of the mod
modId="formations" #mandatory
# The version number of the mod
version="1.0.4" #mandatory
# A display name for the mod
displayName="Formations" #mandatory
# A URL to query for updates for this mod. See the JSON update specification <here>
#updateJSONURL="" #optional
# A URL for the "homepage" for this mod, displayed in the mod UI
displayURL="https://legacy.curseforge.com/minecraft/mc-mods/formations" #optional
# A file name (in the root of the mod JAR) containing a logo for display
logoFile="formations.png" #optional
# A text field displayed in the mod UI
#credits="" #optional
# A text field displayed in the mod UI
authors="SuperMartijn642" #optional
# The description text for the mod (multi line!) (#mandatory)
description='''Formations adds utilities for structure datapacks!'''
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.formations]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[61,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.formations]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.21.11,1.22)"
    ordering="NONE"
    side="BOTH"
